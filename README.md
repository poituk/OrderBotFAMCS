## 🚀 Быстрый старт

### Перед началом

* Установите **Python 3.13+**
* Получите токен бота у [@BotFather](https://t.me/BotFather)

---

### 📦 1. Настройка проекта

1. **Клонируйте репозиторий**

**bash**

```
git clone <repository-url>
cd ORDERBOTFAMCS
```

2. **Создайте файл конфигурации**
   В папке `app` добавьте токен в `config.py`:

**python**

```
# app/config.py
API_TOKEN = "ваш_токен_бота"
```

---

### ▶️ 2. Запуск

#### **Вариант 1: Через UV (рекомендуется)**

**bash**

```
# В папке проекта выполните:
uv run -m app.bot
```

#### **Вариант 2: Через Python + venv**

**bash**

```
# Создайте виртуальное окружение
python -m venv .venv

# Активируйте его
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Запустите бота
python -m app.bot
```
